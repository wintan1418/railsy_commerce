<div class="mx-auto max-w-4xl px-6 py-16">
  <h1 class="font-display text-4xl font-light tracking-tight" style="color: var(--black);"><%= @page.title %></h1>
  <div class="mt-8 prose prose-lg max-w-none" style="color: var(--text-body);">
    <%= simple_format @page.body %>
  </div>
</div>
