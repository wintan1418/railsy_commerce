<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Edit Flash Sale</h1>
  <p class="mt-1 text-sm text-gray-500"><%= @flash_sale.name %> · <%= @flash_sale.status.capitalize %></p>
</div>

<%= render "form", flash_sale: @flash_sale %>
